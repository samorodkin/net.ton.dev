[Unit]
Description=Ton Validator Node
Requires=network.target
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=SCRIPT_DIR/run.sh --service
TimeoutStopSec=60
Restart=always

[Install]
WantedBy=multi-user.target
